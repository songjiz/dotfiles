#!/usr/bin/env bash
# Bash completion support for `git` command.

if [[ -r "/usr/local/etc/bash_completion.d/git-completion.bash" ]]; then
  source "/usr/local/etc/bash_completion.d/git-completion.bash"
fi
